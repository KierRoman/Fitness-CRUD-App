<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Workouts</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css"
        integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg=="
        crossorigin="anonymous" referrerpolicy="no-referrer" />
</head>

<body>
    <a href="/users/<%=user._id%>/workouts/new">Add a Workout</a>


    <% if (workouts.length> 0) { %>
        <ul>
            <% workouts.forEach(workout=> { %>
                <li>
                    <% if(workout.name) {%>
                        <strong>Name:</strong>
                        <a href="/users/<%=user._id%>/workouts/<%=workout._id%>">
                            <%= workout.name %>
                        </a> <br>
                        <strong>Name:</strong>
                        <%= workout.date %> <br>
                            <div><a href="/users/<%=user._id%>/workouts/<%=workout._id%>/edit">
                                    <button style="font-size: 10px;"><i class="fa-solid fa-file-pen"></i></button>
                                </a>
                                <form action="/users/<%=user._id%>/workouts/<%=workout._id%>?_method=DELETE"
                                    method="POST">
                                    <button style="font-size: 10px;"><i class="fa-solid fa-trash"></i></button>
                                </form>
                            </div>
                            <%}%>

                </li>
                <% }) %>

        </ul>
        <% } else { %>
            <p>No workouts available.</p>
            <% } %>
                <%- include('../partials/_backHome.ejs')%>

</body>

</html>